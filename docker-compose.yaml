  services:
  ubuntu-rdp:
    image: danielguerra/ubuntu-xrdp
    container_name: ubuntu_rdp_desktop
    #ports:
    #  - "3389:3389"
    environment:
      - USER_PASSWORD=${USER_PASSWORD}
    restart: unless-stopped